name: evaluation_service
description: gRPC service for BoutiqueScore lead capture (Turso).
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=3.12.2 <4.0.0"

dependencies:
  logging: ^1.3.0
  http: ^1.2.2
  uuid: ^4.5.1
  fence_service:
    path: ../fence_service
  protos_weebi:
    path: ../protos/protos_weebi

dev_dependencies:
  test: ^1.25.8
  lints: ^6.1.0
  fixnum: ^1.1.0
