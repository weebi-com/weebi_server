name: billing_service
description: A starting point for Dart libraries or applications.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=3.12.2 <4.0.0"

dependencies:
  #mongo_dart comes from fence_service
  #fixnum comes from fence_service

  logging: ^1.3.0
  http: ^1.6.0
  country_currency_iso: ^0.1.0
  fence_service:
    path: ../fence_service

dev_dependencies:
  test: ^1.25.8
  lints: ^6.1.0
